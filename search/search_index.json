{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"addlib","text":"<p>Checked integer &amp; float addition with a C core, exposed to Python via a CFFI ABI.</p> <p><code>addlib</code> adds two numbers \u2014 on purpose. It is a worked, end-to-end example of the VERIFHE open-source library roadmap: a C performance core (<code>libadd</code>) bound to a Python API (<code>addlib</code>) through a CFFI ABI, taken through every phase of building a real, security-critical open-source library.</p> <p>The shape is faithful; the payload is trivial. Read it in an afternoon, lift the scaffolding.</p>"},{"location":"#30-seconds","title":"30 seconds","text":"<pre><code>from addlib import add, add_floats\n\nadd(2, 3)              # 5  \u2014 computed in C, overflow-checked\nadd_floats(0.1, 0.2)   # 0.30000000000000004\n</code></pre> <pre><code>pip install addlib     # self-contained wheel, no toolchain needed\n</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":"- **Tutorials** \u2014 learning-oriented. Start at   [Getting started](tutorials/getting-started.md). - **How-to guides** \u2014 task-oriented. E.g.   [Handle overflow safely](how-to/handling-overflow.md),   [Use `libadd` from C](how-to/use-libadd-from-c.md). - **Reference** \u2014 the exact API: [Python](reference/python-api.md) \u00b7   [C](reference/c-api.md). - **Explanation** \u2014 the *why*: [design](explanation/design.md),   [security model](explanation/security-model.md),   [testing](explanation/testing.md)."},{"location":"#safety-in-one-line","title":"Safety in one line","text":"<p><code>add</code> never wraps silently: an out-of-range result raises <code>AdditionOverflowError</code>. See the anti-patterns page for the misuse this prevents.</p>"},{"location":"ROADMAP/","title":"Roadmap mapping","text":"<p><code>addlib</code> is a worked example of the VERIFHE library roadmap (the open-source-library design document this repo accompanies). This page maps each roadmap phase to the concrete artifacts in this repository, and sketches a forward roadmap.</p>"},{"location":"ROADMAP/#what-each-phase-produced","title":"What each phase produced","text":"Phase In the roadmap In <code>addlib</code> 0 \u2014 Foundations License, governance, security intake, CoC, contribution basis <code>LICENSE</code> (Apache-2.0), <code>NOTICE</code>, <code>GOVERNANCE.md</code>, <code>SECURITY.md</code>, <code>CODE_OF_CONDUCT.md</code>, <code>CONTRIBUTING.md</code> (DCO) 1 \u2014 Architecture C/Python boundary, modules, artifact decision, design doc <code>c/</code> core + <code>add.h</code> ABI, CFFI binding, <code>addlib</code> API, design doc, ADRs 2 \u2014 Infra &amp; CI/CD CI, cross-platform wheels, dev env, SBOM <code>pyproject.toml</code>/<code>setup.py</code>, <code>cibuildwheel</code>, <code>Makefile</code>, devcontainer, <code>ci.yml</code>/<code>wheels.yml</code>, Dependabot 3 \u2014 Testing Pyramid, KATs, negative tests, fuzzing, benchmarks unit/property/KAT/negative/ABI tests, libFuzzer target with a 128-bit oracle, <code>pytest-benchmark</code>, nightly Valgrind 4 \u2014 Security Secure-by-construction, supply chain, disclosure, audit security model, disclosure runbook, CodeQL, OpenSSF Scorecard, dependency review 5 \u2014 Releases SemVer, automated signed releases, channels versioning, <code>CHANGELOG.md</code>, <code>release.yaml</code> (SBOM + provenance + Sigstore + Trusted Publishing) 6 \u2014 Docs &amp; site Di\u00e1taxis, docs-as-code, website this MkDocs Material site, <code>README.md</code>, <code>CITATION.cff</code>, runnable examples 7 \u2014 Community On-ramp, channels, RFCs issue/PR templates, this roadmap, RFC template 8 \u2014 Maintenance Bus factor, ADRs, sustainability ADRs + design doc as release-blocking artifacts, <code>MAINTAINERS.md</code>, this page"},{"location":"ROADMAP/#the-one-ordering-the-roadmap-insists-on","title":"The one ordering the roadmap insists on","text":"<p>Testing and security tooling belong at the front of the timeline, not the end. In <code>addlib</code> the test suite, fuzzer, sanitizers, and supply-chain workflows existed before any \"feature\" beyond <code>add</code> \u2014 exactly the roadmap's argument that for a correctness-critical library, testing is the security argument.</p>"},{"location":"ROADMAP/#forward-roadmap-illustrative","title":"Forward roadmap (illustrative)","text":"<ul> <li>0.2 \u2014 a saturating/wrapping <code>add</code> variant (seeds the experimental tier),   behind a clearly-marked import.</li> <li>0.3 \u2014 vectorized addition over buffers (NumPy protocol); may justify a   second published Python artifact.</li> <li>0.x \u2192 1.0 \u2014 stabilize the API; reach <code>1.0</code> aligned with a (hypothetical)   external review, per versioning.</li> <li>C library \u2014 publish <code>libadd</code> to conda-forge / vcpkg if and when C   consumers ask (demand-driven).</li> </ul> <p>Bigger or breaking changes go through the lightweight RFC process.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to this project are documented here.</p> <p>The format is based on Keep a Changelog, and the project adheres to Semantic Versioning. At release time the changelog is generated from Conventional Commits and lightly hand-curated \u2014 auto-generated lists alone make poor release notes.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":""},{"location":"changelog/#010-2026-06-04","title":"0.1.0 \u2014 2026-06-04","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>add(a, b)</code> \u2014 checked signed-64-bit integer addition, computed in C; raises   <code>AdditionOverflowError</code> instead of wrapping.</li> <li><code>add_floats(a, b)</code> \u2014 IEEE-754 double addition, computed in C.</li> <li><code>c_version()</code> and the <code>AdditionOverflowError</code> exception.</li> <li><code>addlib.testing.approx_equal</code> \u2014 tolerance-based float comparison helper.</li> <li>Standalone <code>libadd</code> C library with CMake package config + pkg-config, for   C/C++ consumers (<code>SOVERSION</code> 0).</li> <li>CFFI binding (<code>addlib._add_cffi</code>) that compiles the C core into a   self-contained wheel (no toolchain needed to <code>pip install</code>).</li> <li>Test suite: unit, property-based (Hypothesis), known-answer, adversarial /   negative (release-blocking), and ABI-contract tests.</li> <li>libFuzzer target with a 128-bit differential oracle.</li> <li>CI: lint (ruff/mypy/clang-format), cross-platform test matrix, ASan/UBSan C   build, packaging smoke test; nightly fuzzing, Valgrind, and benchmark trend.</li> <li>Supply chain: CodeQL, OpenSSF Scorecard, dependency review, Dependabot.</li> </ul>"},{"location":"changelog/#security","title":"Security","text":"<ul> <li>Private vulnerability reporting and a 72-hour acknowledgement SLA   (security policy);   disclosure runbook documented.</li> </ul>"},{"location":"adr/","title":"Architecture Decision Records","text":"<p>ADRs capture why a decision was made, so the rationale outlives the people who made it. Per the roadmap's Phase 8.2, these (plus the design doc) are treated as release-blocking artifacts, not optional docs \u2014 they are the project's bus-factor insurance.</p> <p>Format: one short file per decision, numbered, immutable once <code>Accepted</code> (supersede with a new ADR rather than editing).</p> ADR Title Status 0001 Use Apache-2.0 Accepted 0002 Expose the C core via a CFFI ABI Accepted 0003 Monorepo publishing a C library + a Python package Accepted 0004 setuptools+CFFI for the wheel; CMake for the C library Accepted"},{"location":"adr/0001-license-apache-2.0/","title":"ADR-0001: Use the Apache License 2.0","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-04</li> </ul>"},{"location":"adr/0001-license-apache-2.0/#context","title":"Context","text":"<p>We must pick a license before the first public push (Phase 0.1). The candidate audience is broad industrial + community adoption. Options considered: MIT, Apache-2.0, BSD-3, MPL-2.0, LGPL/GPL.</p>"},{"location":"adr/0001-license-apache-2.0/#decision","title":"Decision","text":"<p>License the project under Apache-2.0.</p>"},{"location":"adr/0001-license-apache-2.0/#rationale","title":"Rationale","text":"<ul> <li>Permissive \u2192 zero adoption friction; preferred by corporate legal teams.</li> <li>Explicit patent grant and retaliation clause \u2014 the property MIT/BSD lack,   and the reason the roadmap recommends Apache-2.0 for an industry-aimed crypto   library. (For an addition library the patent surface is nil, but we follow the   recommended default so the example is faithful.)</li> <li>It is the default foundations (Apache, Linux Foundation, OpenSSF) expect.</li> </ul>"},{"location":"adr/0001-license-apache-2.0/#alternatives-close-call","title":"Alternatives / close call","text":"<p>Dual MIT OR Apache-2.0 is the de-facto standard in the ZK/crypto ecosystem and maximizes compatibility. If <code>addlib</code> ever needs to vendor into MIT-only ecosystems, revisit with a follow-up ADR. The cost of dual-licensing is only a little more LICENSE paperwork.</p>"},{"location":"adr/0001-license-apache-2.0/#consequences","title":"Consequences","text":"<ul> <li><code>LICENSE</code> holds the verbatim Apache-2.0 text; <code>NOTICE</code> holds the copyright.</li> <li>Source files carry <code>SPDX-License-Identifier: Apache-2.0</code> headers.</li> <li>CI runs a dependency-license check to ensure no incompatible (e.g. GPL)   dependency sneaks in.</li> </ul>"},{"location":"adr/0002-cffi-binding/","title":"ADR-0002: Expose the C core via a CFFI ABI","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-04</li> </ul>"},{"location":"adr/0002-cffi-binding/#context","title":"Context","text":"<p>The core is pure C. We must choose how Python talks to it (Phase 1.1). Options: pybind11, Cython, CFFI, PyO3+Rust, raw CPython API.</p>"},{"location":"adr/0002-cffi-binding/#decision","title":"Decision","text":"<p>Use CFFI in API, out-of-line mode. A documented C ABI (<code>c/include/add/add.h</code>) is the contract; the bindings sit on top of it.</p>"},{"location":"adr/0002-cffi-binding/#rationale","title":"Rationale","text":"<ul> <li>Best match for a pure-C core (no C++ toolchain dependency pulled in).</li> <li>Keeps the C library reusable independently of Python \u2014 the contract is the   header, not the binding.</li> <li>\"API out-of-line\" compiles a real extension module, so we get a fast, normal   import and self-contained wheels.</li> </ul>"},{"location":"adr/0002-cffi-binding/#alternatives","title":"Alternatives","text":"<ul> <li>pybind11/Cython: pull in C++ or a second language; unnecessary here.</li> <li>PyO3 + Rust: attractive for memory safety, but would mean rewriting the   core in Rust. Out of scope for this example; revisit only if the core moves to   Rust.</li> </ul>"},{"location":"adr/0002-cffi-binding/#consequences","title":"Consequences","text":"<ul> <li>The <code>cdef</code> in <code>_build_ffi.py</code> duplicates the header's declarations; a test   guards that they agree.</li> <li>Adding/changing a C function requires updating both <code>add.h</code> and the <code>cdef</code>.</li> </ul>"},{"location":"adr/0003-monorepo-two-artifacts/","title":"ADR-0003: Monorepo publishing a C library + a Python package","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-04</li> </ul>"},{"location":"adr/0003-monorepo-two-artifacts/#context","title":"Context","text":"<p>The central structural decision (Phase 1.3): one artifact or many? Options: (A) single Python package, (B) many independent packages, (C) one monorepo publishing several artifacts.</p>"},{"location":"adr/0003-monorepo-two-artifacts/#decision","title":"Decision","text":"<p>Option C. One Git repository. It produces two artifacts:</p> <ul> <li><code>libadd</code> \u2014 the standalone C library (CMake + pkg-config).</li> <li><code>addlib</code> \u2014 the Python package, which bundles the compiled C core.</li> </ul> <p>We publish the Python wheel first; the C library's config files ship for direct C consumers. The internal <code>c/</code> vs <code>python/</code> split is kept clean enough to spin the C library out to its own release cadence later without moving code.</p>"},{"location":"adr/0003-monorepo-two-artifacts/#rationale","title":"Rationale","text":"<ul> <li>Atomic cross-cutting refactors (change the ABI and both consumers in one   commit) while the design is still moving.</li> <li>One place for issues, CI, and docs; contributors clone once.</li> <li>Preserves the multi-artifact future without paying coordination cost now \u2014   the roadmap's recommended starting point.</li> </ul>"},{"location":"adr/0003-monorepo-two-artifacts/#consequences","title":"Consequences","text":"<ul> <li>The sdist must include <code>c/</code> so the wheel can compile the core from source.</li> <li>Versioning starts lockstep (one number) and may move to the hybrid scheme   (independent C-library version) if cadences diverge \u2014 see   versioning.md.</li> <li>A future <code>vfhe-ring</code>-style split is a packaging change, not code surgery.</li> </ul>"},{"location":"adr/0004-build-backend/","title":"ADR-0004: setuptools+CFFI for the wheel; CMake for the C library","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-04</li> </ul>"},{"location":"adr/0004-build-backend/#context","title":"Context","text":"<p>Phase 2.2 recommends scikit-build-core (CMake) + cibuildwheel as the build backend. Our binding is CFFI (ADR-0002), which generates and compiles its own extension module. We need a backend that drives CFFI cleanly and still serves C consumers.</p>"},{"location":"adr/0004-build-backend/#decision","title":"Decision","text":"<ul> <li>Python wheel: PEP 517 build via setuptools with CFFI's   <code>cffi_modules</code> integration (a one-line <code>setup.py</code> plus metadata in   <code>pyproject.toml</code>). Wheels are built across platforms with cibuildwheel.</li> <li>Standalone C library: a full CMake build (<code>CMakeLists.txt</code>, install,   export, pkg-config) \u2014 unchanged from the roadmap's intent.</li> </ul>"},{"location":"adr/0004-build-backend/#rationale","title":"Rationale","text":"<ul> <li>CFFI owns extension generation; routing it through scikit-build-core/CMake   adds indirection without benefit for a single-source-file core.</li> <li>cibuildwheel is backend-agnostic, so we keep the recommended wheel matrix   (manylinux, macOS incl. Apple Silicon, Windows) and the sdist fallback.</li> <li>\"CMake is the lingua franca\" is still honored \u2014 for the C artifact, which is   exactly who benefits from it.</li> </ul>"},{"location":"adr/0004-build-backend/#consequences","title":"Consequences","text":"<ul> <li>Two build descriptions coexist: <code>pyproject.toml</code>+<code>setup.py</code> (Python) and   <code>CMakeLists.txt</code> (C). They share the same source in <code>c/</code>.</li> <li>Revisit this ADR if the C core grows multiple translation units, optional   native dependencies, or platform-specific compilation that CMake expresses   better than CFFI's <code>set_source</code> \u2014 at that point scikit-build-core wrapping a   CMake-built <code>libadd</code> becomes the better call.</li> </ul>"},{"location":"explanation/anti-patterns/","title":"Anti-patterns (what not to do)","text":"<p>For an audience with limited domain expertise, \"here is the wrong way\" is as valuable as the how-to (roadmap Phase 6.2). For a real crypto library this page would list parameter choices that silently destroy security. For <code>addlib</code> the stakes are small, but the habit is the point.</p>"},{"location":"explanation/anti-patterns/#assuming-integer-addition-wraps","title":"\u274c Assuming integer addition wraps","text":"<pre><code>add(2**63 - 1, 1)   # does NOT return -2**63; raises AdditionOverflowError\n</code></pre> <p><code>add</code> is checked. If you genuinely want modular/wrapping arithmetic, do it explicitly in Python (<code>(a + b) % 2**64</code>) \u2014 don't expect <code>addlib</code> to wrap.</p>"},{"location":"explanation/anti-patterns/#comparing-float-results-with","title":"\u274c Comparing float results with <code>==</code>","text":"<pre><code>add_floats(0.1, 0.2) == 0.3      # False!  (0.30000000000000004)\n</code></pre> <p>Use a tolerance:</p> <pre><code>from addlib.testing import approx_equal\napprox_equal(add_floats(0.1, 0.2), 0.3)   # True\n</code></pre>"},{"location":"explanation/anti-patterns/#passing-huge-python-ints-and-expecting-a-result","title":"\u274c Passing huge Python ints and expecting a result","text":"<pre><code>add(2**200, 1)   # raises \u2014 operands must fit in signed 64 bits\n</code></pre> <p>Use Python's built-in <code>int</code> for arbitrary precision. <code>addlib</code> is a 64-bit C core and says so loudly rather than truncating.</p>"},{"location":"explanation/anti-patterns/#reaching-into-private-internals","title":"\u274c Reaching into private internals","text":"<pre><code>from addlib._add_cffi import lib   # private! may change without notice\n</code></pre> <p>The public API is <code>addlib.add</code>, <code>addlib.add_floats</code>, <code>addlib.c_version</code>, <code>addlib.AdditionOverflowError</code>. Anything with a leading underscore (<code>_add_cffi</code>, <code>_build_ffi</code>) is internal \u2014 see the design doc.</p>"},{"location":"explanation/anti-patterns/#catching-exception-to-handle-overflow","title":"\u274c Catching <code>Exception</code> to \"handle overflow\"","text":"<pre><code>try:\n    result = add(a, b)\nexcept Exception:   # too broad \u2014 hides bugs\n    result = 0\n</code></pre> <p>Catch the specific <code>AdditionOverflowError</code> (or <code>OverflowError</code>), so real bugs still surface.</p>"},{"location":"explanation/design/","title":"Technical Design Document","text":"<p>This is the Task 1.1 deliverable analogue: a living design document, reviewed like code. It is the home for the C-ABI contract, the module dependency graph, the artifact decision, the stability tiering, and the public-API surface.</p>"},{"location":"explanation/design/#1-scope","title":"1. Scope","text":"<p><code>addlib</code> adds two numbers. It exists as a worked example of the VERIFHE open-source library roadmap: a C performance core exposed to Python through a CFFI ABI, carried through every phase (licensing, CI, testing, security, release, docs, community, maintenance).</p> <p>Everywhere the real VERIFHE library would have RNS arithmetic, CKKS, PIOPs and a SNARK, <code>addlib</code> has <code>add_i64</code> and <code>add_f64</code>. The shape is faithful; the payload is trivial on purpose.</p>"},{"location":"explanation/design/#2-the-cpython-boundary","title":"2. The C/Python boundary","text":"<p>The split, per Phase 1.1:</p> <ul> <li>C does the arithmetic (<code>c/src/add.c</code>). It is the performance core and the   independently reusable artifact.</li> <li>Python provides the ergonomic, misuse-resistant API   (<code>python/src/addlib/core.py</code>).</li> <li>They talk over a CFFI ABI (<code>python/src/addlib/_build_ffi.py</code>), chosen   because the core is pure C (no C++). This matches the roadmap's recommendation   (\"clean C ABI exposed via CFFI ... best match for a pure-C core\").</li> </ul> <pre><code>            \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n users  \u2500\u2500\u25b6 \u2502 addlib (Python)             \u2502   high-level, validated, typed\n            \u2502   core.py  testing.py       \u2502\n            \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502  CFFI (API, out-of-line)\n            \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n            \u2502 addlib._add_cffi (compiled) \u2502   generated glue\n            \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502  C ABI  (add.h)  \u25c0\u2500\u2500 the contract\n            \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n            \u2502 libadd (C)                  \u2502   also usable standalone via CMake\n            \u2502   add.c  add.h              \u2502\n            \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"explanation/design/#3-the-c-abi-contract","title":"3. The C-ABI contract","text":"<p><code>c/include/add/add.h</code> is the only surface the bindings depend on, and the only thing C consumers link against. It is a first-class deliverable.</p> Symbol Signature Contract <code>add_i64</code> <code>add_status_t add_i64(int64_t, int64_t, int64_t*)</code> Checked add; <code>ADD_ERR_OVERFLOW</code> if the true sum leaves <code>int64</code>; <code>ADD_ERR_NULL</code> if out is NULL; never UB. <code>add_f64</code> <code>double add_f64(double, double)</code> IEEE-754 sum; NaN/Inf propagate. <code>add_version</code> <code>const char *add_version(void)</code> Static version string. <code>add_status_t</code> enum <code>{ADD_OK=0, ADD_ERR_NULL=1, ADD_ERR_OVERFLOW=2}</code> Values are ABI; never renumber, only append. <p>The CFFI <code>cdef</code> block is the Python-visible projection of this header. The two must agree; <code>tests/test_abi_contract.py</code> checks the projection compiles and the symbols/return types behave as declared.</p> <p>ABI vs API. For C consumers the ABI (struct layout, enum values, signatures) is a separate contract from the source API, versioned by <code>SOVERSION</code> (= MAJOR). Breaking it is a MAJOR event even if Python is unaffected. See versioning.md.</p>"},{"location":"explanation/design/#4-module-artifact-map","title":"4. Module / artifact map","text":"<p>Two artifacts from one monorepo (Phase 1.3, Option C):</p> Artifact What Consumers Build <code>libadd</code> C library + headers + CMake/pkg-config C/C++ projects CMake <code>addlib</code> Python package bundling the C core Python users (<code>pip install addlib</code>) setuptools + CFFI \u2192 cibuildwheel <p>We start by publishing the Python wheel (which bundles the C core, so a user needs no toolchain) and ship the C library's CMake/pkg-config files for the minority who want to link <code>libadd</code> directly. This is the roadmap's recommended starting point: \"a <code>libvfhe</code> C library plus a <code>verifhe</code> Python umbrella to begin with, with internal module boundaries clean enough to spin out ... later.\"</p>"},{"location":"explanation/design/#5-stability-tiering","title":"5. Stability tiering","text":"<p>Mirrors Phase 1.2's primitives-vs-protocols gradient:</p> Tier Members Stability promise stable <code>add_i64</code>, <code>add_f64</code>, <code>add_version</code> and their Python wrappers SemVer-guaranteed from 1.0; ABI guaranteed by <code>SOVERSION</code>. experimental (none yet) \u2014 e.g. a future <code>add_saturating</code>, vector add May change in MINOR while pre-1.0; lives behind a clearly-marked import. <p>The tiering predicts where breaking changes originate and decides what earns a stability promise first.</p>"},{"location":"explanation/design/#6-public-api-surface-v0","title":"6. Public API surface (v0)","text":"<p>Python (<code>addlib</code>): <code>add</code>, <code>add_floats</code>, <code>c_version</code>, <code>AdditionOverflowError</code>, <code>__version__</code>. Plus <code>addlib.testing.approx_equal</code> for tolerance-based float comparison.</p> <p>C (<code>libadd</code>): the three functions and one enum in <code>add.h</code>.</p> <p>Anything not listed is private and may change without notice (CFFI internals, <code>_build_ffi</code>, <code>_add_cffi</code>).</p>"},{"location":"explanation/design/#7-build-backend-decision-a-deliberate-deviation-documented","title":"7. Build-backend decision (a deliberate deviation, documented)","text":"<p>The roadmap recommends scikit-build-core (CMake) + cibuildwheel. We deviate for the Python wheel and use setuptools + CFFI's own build integration, because:</p> <ol> <li>The binding is CFFI, and CFFI generates and compiles its own extension    module. Driving that through scikit-build-core/CMake adds a layer without    benefit for a single-source-file core.</li> <li>We still honor \"CMake is the lingua franca\": <code>libadd</code> has a full CMake build    with install/export/pkg-config for C consumers.</li> <li>cibuildwheel is backend-agnostic, so we keep the recommended wheel matrix.</li> </ol> <p>This is recorded as ADR-0004. If the C core grows multiple translation units or optional native dependencies, revisit scikit-build-core.</p>"},{"location":"explanation/design/#8-open-questions-future","title":"8. Open questions / future","text":"<ul> <li>A saturating or wrapping <code>add</code> variant (would seed the <code>experimental</code> tier).</li> <li>Vectorized <code>add</code> over arrays (NumPy buffer protocol) \u2014 would change the   binding ergonomics and might argue for a second published Python artifact.</li> <li>Whether to expose <code>libadd</code> on conda-forge / vcpkg (demand-driven; Phase 5.6).</li> </ul>"},{"location":"explanation/disclosure-runbook/","title":"Vulnerability disclosure runbook","text":"<p>Write the workflow down before you need it \u2014 a panic during a live disclosure is how mistakes happen (Phase 4.3). This is the maintainer-facing companion to the user-facing SECURITY.md.</p>"},{"location":"explanation/disclosure-runbook/#roles","title":"Roles","text":"<ul> <li>Triager \u2014 first maintainer to see the report; acknowledges and does the   initial assessment.</li> <li>Fix owner \u2014 maintainer who develops the patch (often the triager).</li> <li>Release manager \u2014 maintainer who cuts the patched release and publishes   the advisory.</li> </ul> <p>(On a small team one person may wear all three hats; name them per incident in the private advisory thread.)</p>"},{"location":"explanation/disclosure-runbook/#timeline","title":"Timeline","text":"When Action T + 0 Report arrives via GitHub Private Vulnerability Reporting or <code>security@</code>. \u2264 72 h Triager acknowledges receipt to the reporter. \u2264 7 d Triage: confirm/repro, assess severity (CVSS), decide in/out of scope. during Open a draft GitHub Security Advisory (private). Work the fix on a private fork/branch linked to the advisory. during If warranted, request a CVE through GitHub (GitHub is a CNA). \u2264 30 d Fix ready, reviewed, regression test added (this test becomes a permanent gate). embargo end Default embargo 90 days or sooner by agreement with the reporter. release Release manager ships the patched version (see releasing.md), then publishes the advisory and credits the reporter."},{"location":"explanation/disclosure-runbook/#fix-release-mechanics","title":"Fix &amp; release mechanics","text":"<ol> <li>Land the fix on the supported line(s). Pre-1.0 that is latest only; once    we maintain older lines, backport to their release branches.</li> <li>The fix ships as a PATCH release and is announced via the advisory.</li> <li>Update <code>CHANGELOG.md</code> under a <code>### Security</code> heading,    referencing the CVE/advisory ID.</li> <li>Bump <code>SECURITY.md</code>'s supported-versions table if needed.</li> </ol>"},{"location":"explanation/disclosure-runbook/#after-the-fact","title":"After the fact","text":"<ul> <li>Hold a short blameless retro: how did it get in, did a test gap let it   through, what guard prevents recurrence?</li> <li>Keep the regression test forever.</li> </ul>"},{"location":"explanation/disclosure-runbook/#coordinates","title":"Coordinates","text":"<ul> <li>Private reporting: repo Security \u2192 Report a vulnerability.</li> <li>Email fallback: <code>rosualinpetru@gmail.com</code> (placeholder \u2014 replace before going   public).</li> </ul>"},{"location":"explanation/maintenance/","title":"Maintenance &amp; sustainability","text":"<p>The phase with no deadline and no glory \u2014 and the one that decides whether a library outlives the burst of effort that created it. <code>addlib</code> is an example, so this page is partly a checklist for whoever maintains a library shaped like this.</p>"},{"location":"explanation/maintenance/#the-ongoing-load","title":"The ongoing load","text":"<ul> <li>Issue triage. Label (<code>bug</code>, <code>enhancement</code>, <code>triage</code>, <code>good-first-issue</code>),   prioritize, and apply a stale policy. Triage debt compounds quietly. Aim for a   fast first response, even just \"thanks, we'll look this week.\"</li> <li>Dependency maintenance. Dependabot opens grouped update PRs weekly; CI must   pass before merge. The runtime dependency surface is intentionally tiny   (<code>cffi</code>) \u2014 every dependency is attack surface.</li> <li>Toolchain drift. New Python versions, compilers, and CI OS images break   native builds over time. The wheel matrix (<code>cibuildwheel</code>) and the test matrix   need periodic bumps just to keep working. Add new CPython versions to   <code>pyproject.toml</code>'s <code>[tool.cibuildwheel] build</code> and the CI matrix.</li> <li>Performance vigilance. The nightly benchmark JSON is there to be watched,   not just collected \u2014 a regression in the CFFI-boundary cost should be noticed.</li> <li>Security response. Keep the disclosure runbook   warm and the <code>security@</code> intake monitored.</li> </ul>"},{"location":"explanation/maintenance/#bus-factor-continuity","title":"Bus factor &amp; continuity","text":"<p>The acute risk for any small project: knowledge living in one or two heads. Mitigations, all already in place here:</p> <ul> <li>Write the rationale down. The design doc and   ADRs are treated as release-blocking artifacts, not   optional extras \u2014 the highest-leverage continuity investment.</li> <li>Grow maintainers. The contributor on-ramp (<code>CONTRIBUTING.md</code>,   good-first-issues, responsive review) is the succession plan. Trusted   contributors earn merge rights per   GOVERNANCE.md.</li> <li>Spread the keys. The maintainer committee holds merge/release rights so no   single departure halts the project; keep   <code>MAINTAINERS.md</code>   current.</li> </ul>"},{"location":"explanation/maintenance/#funding-institutional-sustainability","title":"Funding &amp; institutional sustainability","text":"<p>For a real library (not this example), the menu is: follow-on grants; institutional hosting; industry collaboration that converts into co-maintenance or sponsored features; and supplementary sponsorship (<code>FUNDING.yml</code>). Sponsorship alone rarely sustains a specialist library \u2014 treat it as a supplement, not the plan.</p> <p>Foundation home (close call). Joining a neutral umbrella adds credibility and shared infrastructure but also process; it is usually premature early. Revisit only if/when independent co-maintainers materialize and want neutral governance.</p>"},{"location":"explanation/maintenance/#a-note-on-ordering","title":"A note on ordering","text":"<p>Most of what makes maintenance survivable was front-loaded: tests, fuzzing, sanitizers, supply-chain workflows, and written-down decisions all existed before the project had more than one function. That is deliberate \u2014 see the roadmap mapping.</p>"},{"location":"explanation/releasing/","title":"Releasing","text":"<p>Releases are tag-triggered and automated (Phase 5.4). The human does two things: bump the version and push a tag. CI does the rest \u2014 build, sign, publish \u2014 which removes the manual errors that creep into hand-cut crypto-library releases (forgetting to sign, shipping an unbuilt platform, a mismatched changelog).</p>"},{"location":"explanation/releasing/#one-time-setup","title":"One-time setup","text":"<ul> <li>PyPI Trusted Publishing: register the repo + <code>release.yaml</code> workflow as a   trusted publisher on PyPI (OIDC). No long-lived API token lives in the repo.</li> <li>Branch protection on <code>main</code>: required review, green CI, signed commits.</li> </ul>"},{"location":"explanation/releasing/#release-checklist","title":"Release checklist","text":"<ol> <li>Ensure <code>main</code> is green and the changelog <code>Unreleased</code>    section reflects what is shipping.</li> <li>Bump the version in both places that must agree:</li> <li><code>pyproject.toml</code> \u2192 <code>version</code></li> <li><code>c/include/add/add.h</code> \u2192 <code>ADDLIB_VERSION_*</code> / <code>ADDLIB_VERSION_STRING</code>    (<code>test_version_consistency</code> and <code>test_version_symbol</code> fail if they drift.)</li> <li>Move <code>Unreleased</code> \u2192 <code>[X.Y.Z] \u2014 DATE</code> in the changelog; add the compare link.</li> <li>Commit (<code>-s</code>, signed): <code>git commit -S -s -m \"chore(release): vX.Y.Z\"</code>.</li> <li>Tag and push: <code>git tag -s vX.Y.Z -m \"vX.Y.Z\" &amp;&amp; git push --tags</code>.</li> </ol>"},{"location":"explanation/releasing/#what-the-tag-triggers-releaseyaml","title":"What the tag triggers (<code>release.yaml</code>)","text":"<ol> <li>Build wheels (cibuildwheel: manylinux, macOS incl. Apple Silicon,    Windows) and an sdist; each wheel is import-smoke-tested.</li> <li>SBOM (CycloneDX) generated for the release.</li> <li>Provenance attestation (SLSA / GitHub artifact attestations) for every    artifact.</li> <li>Publish to PyPI via Trusted Publishing.</li> <li>Sign artifacts with Sigstore and attach signatures + SBOM + checksums to    a GitHub Release, with notes derived from Conventional Commits.</li> </ol>"},{"location":"explanation/releasing/#cadence","title":"Cadence","text":"<p>Release when there is something worth releasing (not on a rigid clock) \u2014 but always be able to ship a security patch fast (the disclosure runbook drives those out-of-band).</p>"},{"location":"explanation/releasing/#distribution-channels","title":"Distribution channels","text":"<ul> <li>PyPI \u2014 wheels + sdist, from day one.</li> <li>GitHub Releases \u2014 canonical home for signed artifacts, SBOM, checksums.</li> <li>conda-forge \u2014 add a feedstock around the first stable release; much of the   scientific-Python audience lives in conda and won't build native extensions by   hand. (Demand-driven; not set up yet.)</li> <li>vcpkg / Conan for <code>libadd</code> \u2014 only if/when C consumers ask. Don't pre-build.</li> </ul>"},{"location":"explanation/security-model/","title":"Security model","text":"<p>A short, honest statement of what <code>addlib</code> does and does not protect \u2014 the Phase 4.1 discipline. For a cryptographic library this section pre-answers the questions every auditor asks; for an addition library it is deliberately small, but the structure is the point.</p>"},{"location":"explanation/security-model/#what-the-library-does","title":"What the library does","text":"<p><code>addlib</code> computes <code>a + b</code> for signed 64-bit integers (checked) and IEEE-754 doubles (approximate). That is the entire functional surface.</p>"},{"location":"explanation/security-model/#threat-model","title":"Threat model","text":"Question Answer Does it process secrets? No. Inputs are ordinary numbers, not keys or plaintexts. Is timing/side-channel resistance required? No \u2014 there are no secrets, so there is nothing to leak through timing. <code>add_i64</code> does branch on values (overflow check); this is fine here and would not be acceptable for a secret-dependent operation. The point is documented so the assumption is explicit. What can untrusted input do? The only untrusted-input path is the C function turning bytes/arguments into a result. The risk class is memory safety / undefined behavior, not confidentiality. What is the integrity guarantee? <code>add_i64</code> either returns the mathematically exact sum or signals <code>ADD_ERR_OVERFLOW</code>; it never returns a wrong or silently-wrapped value. <code>add_floats</code> returns the correctly-rounded IEEE-754 sum."},{"location":"explanation/security-model/#what-is-in-scope","title":"What is in scope","text":"<ul> <li>Memory safety of the C core. Mitigated by: a total implementation (no   input triggers UB), ASan/UBSan in CI, Valgrind nightly, and a libFuzzer target   with a 128-bit oracle.</li> <li>Correctness / \"soundness\". Mitigated by KATs, property-based tests, and   release-blocking negative tests (overflow and bad input must be rejected).</li> <li>Supply-chain integrity. This is the part that genuinely matters even for a   trivial library \u2014 see supply chain, below.</li> </ul>"},{"location":"explanation/security-model/#what-is-out-of-scope","title":"What is out of scope","text":"<ul> <li>Confidentiality / side-channel resistance (no secrets).</li> <li>Arbitrary-precision arithmetic (use Python <code>int</code> for that; we bound to int64   on purpose and reject out-of-range inputs loudly).</li> <li>Concurrency hazards: the functions are pure and reentrant; there is no shared   state.</li> </ul>"},{"location":"explanation/security-model/#constant-time-note","title":"Constant-time note","text":"<p>No operation in <code>addlib</code> is constant-time, and none needs to be, because none touches a secret. If a future variant ever operates on secret data, it must be re-evaluated against a constant-time requirement and this document updated.</p>"},{"location":"explanation/security-model/#supply-chain","title":"Supply chain","text":"<p>The realistic attack surface for a tiny dependency is the build and distribution path, not the code. Controls (Phase 4.2):</p> <ul> <li>Signed releases via Sigstore/cosign + published checksums.</li> <li>PyPI Trusted Publishing (OIDC) \u2014 no long-lived token.</li> <li>SBOM (CycloneDX) attached to every release.</li> <li>Provenance attestations (SLSA / GitHub artifact attestations).</li> <li>Branch protection + signed commits + 2FA for maintainers.</li> <li>Dependency scanning (Dependabot) and a minimal dependency surface   (runtime dep: just <code>cffi</code>).</li> <li>OpenSSF Scorecard running on the repo.</li> </ul> <p>See releasing.md and the workflows in <code>.github/workflows/</code>.</p>"},{"location":"explanation/security-model/#audit-status","title":"Audit status","text":"<p>Unaudited. This is an example project. A real library would state here what has and has not been independently reviewed, and would not claim production-readiness before a focused external review of the soundness-critical code. Overclaiming is worse than disclosing the gap.</p>"},{"location":"explanation/testing/","title":"Testing strategy","text":"<p>For a security-critical library, testing is part of the security argument, not just QA. <code>addlib</code> models that discipline at small scale.</p>"},{"location":"explanation/testing/#the-pyramid","title":"The pyramid","text":"Layer Where What it does Unit <code>python/tests/test_addition.py</code>, <code>c/tests/test_add.c</code> Every function, happy path + edges. Known-Answer Tests <code>python/tests/test_kat.py</code> Fixed input\u2192output vectors; the crypto layer ordinary software skips. Property-based <code>python/tests/test_properties.py</code> Invariants over a huge space (Hypothesis), cross-validated against Python's exact integer arithmetic. Negative / adversarial <code>python/tests/test_negative.py</code> Must-reject cases: overflow, out-of-range, wrong types. Release-blocking. ABI contract <code>python/tests/test_abi_contract.py</code> Exercises the C ABI directly, incl. the NULL-output path."},{"location":"explanation/testing/#release-blocking-gates","title":"Release-blocking gates","text":"<p>The <code>negative</code> and <code>kat</code> markers flag tests that must never be weakened to make a change pass \u2014 the analogue of the roadmap's \"treat a soundness regression like a crash.\" Run just those:</p> <pre><code>pytest -m negative\npytest -m kat\n</code></pre>"},{"location":"explanation/testing/#c-correctness-under-sanitizers","title":"C correctness under sanitizers","text":"<p>The C tests run under AddressSanitizer + UndefinedBehaviorSanitizer in CI and under Valgrind nightly. The implementation is written to be total \u2014 no input triggers undefined behavior \u2014 which is the property the fuzzer verifies.</p>"},{"location":"explanation/testing/#fuzzing","title":"Fuzzing","text":"<p><code>c/fuzz/fuzz_add.c</code> is a libFuzzer target over the \"bytes \u2192 result\" path (the deserialization analogue, where memory-safety CVEs live). It uses a 128-bit differential oracle: <code>__int128</code> addition can't overflow two <code>int64</code> values, so it pins the exact expected status and result for every input. Runs nightly; a real project would also apply to OSS-Fuzz.</p> <p>Run it locally with <code>make fuzz</code>. On macOS, Apple's bundled clang does not ship the libFuzzer runtime, so use Homebrew LLVM:</p> <pre><code>brew install llvm\nmake fuzz FUZZ_CC=\"$(brew --prefix llvm)/bin/clang\"\n</code></pre>"},{"location":"explanation/testing/#correctness-under-approximation","title":"Correctness under approximation","text":"<p><code>add_floats</code> is approximate (IEEE-754 rounding). All float comparisons go through <code>addlib.testing.approx_equal</code> with one documented tolerance, so \"correct\" has a single meaning \u2014 the discipline a CKKS-style approximate library needs, in miniature.</p>"},{"location":"explanation/testing/#benchmarks","title":"Benchmarks","text":"<p><code>benchmarks/bench_addition.py</code> tracks per-call cost with <code>pytest-benchmark</code>; the nightly job stores the JSON so trends are visible over time.</p>"},{"location":"explanation/versioning/","title":"Versioning policy","text":""},{"location":"explanation/versioning/#scheme-semantic-versioning","title":"Scheme: Semantic Versioning","text":"<p><code>addlib</code> uses SemVer \u2014 <code>MAJOR.MINOR.PATCH</code>:</p> <ul> <li>MAJOR \u2014 backward-incompatible API (or C ABI) change.</li> <li>MINOR \u2014 backward-compatible additions.</li> <li>PATCH \u2014 backward-compatible fixes.</li> </ul>"},{"location":"explanation/versioning/#the-0x-signal","title":"The 0.x signal","text":"<p>We are pre-1.0. While <code>0.x</code>, a MINOR bump may break the API \u2014 this is honest for a young library. We will stay <code>0.x</code> until the API is genuinely stable, and will not let any deadline force a premature <code>1.0</code>. <code>1.0</code> arrives when we can commit to not breaking the public API casually.</p>"},{"location":"explanation/versioning/#stability-tiering-decides-what-stabilizes-first","title":"Stability tiering decides what stabilizes first","text":"<p>Per the design doc, <code>add_i64</code>/<code>add_f64</code> (the stable tier) can earn a stability promise before any future experimental variant. The tier predicts where breaking changes originate.</p>"},{"location":"explanation/versioning/#three-contracts-versioned-separately","title":"Three contracts, versioned separately","text":"<ol> <li>Python API \u2014 the functions in <code>addlib</code>. Governed by SemVer above.</li> <li>C ABI \u2014 struct layout, enum values, and signatures in <code>add.h</code>. Versioned    by the library <code>SOVERSION</code> (currently <code>0</code>), which is distinct from the    release version. Breaking the ABI (e.g. renumbering <code>add_status_t</code>, changing a    signature) is a MAJOR event for C consumers even if the Python API is    untouched. This is a dimension Python-only projects never face.</li> <li>Wire / serialization format \u2014 not applicable to <code>addlib</code> (it serializes    nothing). Recorded here because the principle matters for the real library: a    proof/ciphertext serialized by one version must be verifiable by a compatible    one, or the incompatibility must be loud. Version such a format with its own    explicit number, independent of the code API.</li> </ol>"},{"location":"explanation/versioning/#security-patches","title":"Security patches","text":"<p>A fix for a memory-safety or correctness (\"soundness\") bug ships as a PATCH, is announced via a security advisory, and \u2014 once we support older lines \u2014 may be backported. See the disclosure runbook.</p>"},{"location":"explanation/versioning/#single-vs-multi-artifact-versioning","title":"Single- vs multi-artifact versioning","text":"<p>Today we ship effectively one product (the <code>addlib</code> wheel bundles the C core), so we version lockstep: one number, one tag, one changelog. Simple to reason about.</p> <p>If/when <code>libadd</code> gains an independent release cadence (ADR-0003), we move to the hybrid scheme the roadmap recommends:</p> <ul> <li>version the stable C library independently (it changes slowly, earns long-lived   guarantees);</li> <li>let research-active / experimental packages move together;</li> <li>publish a one-line compatibility statement per release (e.g.   <code>addlib 0.5 requires libadd &gt;= 0.3, &lt; 0.4</code>) and check the matrix in CI.</li> </ul> <p>Start simple; split when the cadence pain is real, not anticipated.</p>"},{"location":"explanation/versioning/#deprecation-policy","title":"Deprecation policy","text":"<p>Never break silently. To remove something: announce it, provide a migration path, keep the old path working with a <code>DeprecationWarning</code> for at least one MINOR cycle, then remove on a MAJOR. Every breaking change ships with an upgrade note in the changelog.</p>"},{"location":"explanation/versioning/#supported-versions","title":"Supported versions","text":"<p>Pre-1.0: latest <code>0.x</code> only receives security fixes (see SECURITY.md). A support window and backport policy will be defined at 1.0 \u2014 we will not over-promise support we cannot staff.</p>"},{"location":"how-to/handling-overflow/","title":"How to handle overflow safely","text":"<p><code>add</code> operates on signed 64-bit integers. When the true sum is outside <code>[-2**63, 2**63 - 1]</code>, it raises <code>AdditionOverflowError</code> instead of returning a wrong (wrapped) value.</p>"},{"location":"how-to/handling-overflow/#catch-it","title":"Catch it","text":"<pre><code>from addlib import add, AdditionOverflowError\n\ndef safe_add(a: int, b: int) -&gt; int | None:\n    try:\n        return add(a, b)\n    except AdditionOverflowError:\n        return None\n</code></pre>"},{"location":"how-to/handling-overflow/#it-is-an-overflowerror","title":"It is an <code>OverflowError</code>","text":"<p><code>AdditionOverflowError</code> subclasses the built-in <code>OverflowError</code>, so existing handlers keep working:</p> <pre><code>try:\n    add(2**63 - 1, 1)\nexcept OverflowError:\n    ...  # also catches AdditionOverflowError\n</code></pre>"},{"location":"how-to/handling-overflow/#out-of-range-inputs-raise-too","title":"Out-of-range inputs raise too","text":"<p>You cannot sneak an out-of-range operand in \u2014 it is rejected before the C call:</p> <pre><code>add(2**63, 0)   # raises AdditionOverflowError: a is outside the signed 64-bit range\n</code></pre>"},{"location":"how-to/handling-overflow/#need-unbounded-integers","title":"Need unbounded integers?","text":"<p>Use Python's built-in <code>int</code> \u2014 it is arbitrary precision. <code>addlib</code> deliberately bounds to 64 bits (that is what the C core is) and refuses to pretend otherwise.</p> <pre><code>a, b = 2**200, 2**200\na + b           # fine in pure Python\nadd(a, b)       # raises \u2014 by design\n</code></pre>"},{"location":"how-to/use-libadd-from-c/","title":"How to use <code>libadd</code> from C/C++","text":"<p>The C core is a first-class artifact. You can link it without touching Python.</p>"},{"location":"how-to/use-libadd-from-c/#build-and-install","title":"Build and install","text":"<pre><code>cmake -S . -B build -DCMAKE_INSTALL_PREFIX=/usr/local\ncmake --build build\ncmake --install build      # installs lib, headers, CMake config, pkg-config\n</code></pre>"},{"location":"how-to/use-libadd-from-c/#from-a-cmake-project","title":"From a CMake project","text":"<pre><code>find_package(addlib CONFIG REQUIRED)\ntarget_link_libraries(myapp PRIVATE addlib::add)\n</code></pre>"},{"location":"how-to/use-libadd-from-c/#from-a-plain-makefile-pkg-config","title":"From a plain Makefile (pkg-config)","text":"<pre><code>cc myapp.c $(pkg-config --cflags --libs libadd) -o myapp\n</code></pre>"},{"location":"how-to/use-libadd-from-c/#minimal-program","title":"Minimal program","text":"<pre><code>#include &lt;add/add.h&gt;\n#include &lt;stdint.h&gt;\n#include &lt;stdio.h&gt;\n\nint main(void) {\n    int64_t out;\n    switch (add_i64(20, 22, &amp;out)) {\n        case ADD_OK:           printf(\"%lld\\n\", (long long)out); break;\n        case ADD_ERR_OVERFLOW: fprintf(stderr, \"overflow\\n\");    return 1;\n        case ADD_ERR_NULL:     fprintf(stderr, \"null out\\n\");    return 1;\n    }\n    return 0;\n}\n</code></pre> <p>A runnable copy is in <code>examples/use_libadd.c</code>.</p>"},{"location":"how-to/use-libadd-from-c/#abi-stability","title":"ABI stability","text":"<p><code>libadd</code>'s ABI is versioned by its <code>SOVERSION</code> (currently <code>0</code>), separate from the release version. Breaking it (changing a signature, renumbering <code>add_status_t</code>) is a MAJOR-level event for C consumers \u2014 see versioning.</p>"},{"location":"reference/c-api/","title":"C API reference","text":"<p>The stable C ABI. The single source of truth is <code>c/include/add/add.h</code>; this page summarizes it. (A real project would bridge in Doxygen output here.)</p>"},{"location":"reference/c-api/#status-codes","title":"Status codes","text":"<pre><code>typedef enum add_status {\n    ADD_OK           = 0,  // success; output written\n    ADD_ERR_NULL     = 1,  // a required output pointer was NULL\n    ADD_ERR_OVERFLOW = 2   // the true result is out of range\n} add_status_t;\n</code></pre> <p>Values are part of the ABI \u2014 never renumbered, only appended.</p>"},{"location":"reference/c-api/#functions","title":"Functions","text":""},{"location":"reference/c-api/#add_i64","title":"<code>add_i64</code>","text":"<pre><code>add_status_t add_i64(int64_t a, int64_t b, int64_t *out);\n</code></pre> <p>Adds <code>a + b</code> with overflow checking. On success writes the sum to <code>*out</code> and returns <code>ADD_OK</code>. On overflow returns <code>ADD_ERR_OVERFLOW</code> and leaves <code>*out</code> unmodified. Returns <code>ADD_ERR_NULL</code> if <code>out</code> is <code>NULL</code>. Never invokes undefined behavior on any input.</p>"},{"location":"reference/c-api/#add_f64","title":"<code>add_f64</code>","text":"<pre><code>double add_f64(double a, double b);\n</code></pre> <p>Returns the IEEE-754 double-precision sum. Infinities and NaN propagate; no exception is raised.</p>"},{"location":"reference/c-api/#add_version","title":"<code>add_version</code>","text":"<pre><code>const char *add_version(void);\n</code></pre> <p>Returns the version string (e.g. <code>\"0.1.0\"</code>) with static storage duration; do not free it.</p>"},{"location":"reference/c-api/#versioning-macros","title":"Versioning macros","text":"<pre><code>#define ADDLIB_VERSION_MAJOR  0\n#define ADDLIB_VERSION_MINOR  1\n#define ADDLIB_VERSION_PATCH  0\n#define ADDLIB_VERSION_STRING \"0.1.0\"\n</code></pre>"},{"location":"reference/python-api/","title":"Python API reference","text":"<p>Auto-generated from docstrings (mkdocstrings), so it cannot drift from the code.</p>"},{"location":"reference/python-api/#functions","title":"Functions","text":""},{"location":"reference/python-api/#addlib.add","title":"addlib.add","text":"<pre><code>add(a, b)\n</code></pre> <p>Return <code>a + b</code> for two signed 64-bit integers, computed in C.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>int</code> <p>First addend. Must fit in signed 64 bits.</p> required <code>b</code> <code>int</code> <p>Second addend. Must fit in signed 64 bits.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The exact sum <code>a + b</code>.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>a</code> or <code>b</code> is not an :class:<code>int</code>.</p> <code>AdditionOverflowError</code> <p>If an input is outside the signed 64-bit range, or if the true sum overflows it.</p> Example <p>from addlib import add add(2, 3) 5 add(-1, 1) 0</p> Source code in <code>python/src/addlib/core.py</code> <pre><code>def add(a: int, b: int) -&gt; int:\n    \"\"\"Return ``a + b`` for two signed 64-bit integers, computed in C.\n\n    Args:\n        a: First addend. Must fit in signed 64 bits.\n        b: Second addend. Must fit in signed 64 bits.\n\n    Returns:\n        The exact sum ``a + b``.\n\n    Raises:\n        TypeError: If ``a`` or ``b`` is not an :class:`int`.\n        AdditionOverflowError: If an input is outside the signed 64-bit range,\n            or if the true sum overflows it.\n\n    Example:\n        &gt;&gt;&gt; from addlib import add\n        &gt;&gt;&gt; add(2, 3)\n        5\n        &gt;&gt;&gt; add(-1, 1)\n        0\n    \"\"\"\n    a = _as_int(\"a\", a)\n    b = _as_int(\"b\", b)\n    for name, value in ((\"a\", a), (\"b\", b)):\n        if not (_INT64_MIN &lt;= value &lt;= _INT64_MAX):\n            raise AdditionOverflowError(\n                f\"{name}={value} is outside the signed 64-bit range \"\n                f\"[{_INT64_MIN}, {_INT64_MAX}]\"\n            )\n\n    out = ffi.new(\"int64_t *\")\n    status = lib.add_i64(a, b, out)\n    if status == lib.ADD_ERR_OVERFLOW:\n        raise AdditionOverflowError(f\"{a} + {b} overflows the signed 64-bit range\")\n    if status != lib.ADD_OK:\n        # ADD_ERR_NULL is unreachable here (we always pass a valid pointer);\n        # surface anything unexpected loudly rather than returning garbage.\n        raise RuntimeError(f\"add_i64 returned unexpected status {status}\")\n    return int(out[0])\n</code></pre>"},{"location":"reference/python-api/#addlib.add_floats","title":"addlib.add_floats","text":"<pre><code>add_floats(a, b)\n</code></pre> <p>Return <code>a + b</code> for two real numbers, computed in C as IEEE-754 doubles.</p> <p>Follows IEEE-754 semantics: <code>add_floats(float(\"inf\"), 1.0)</code> is <code>inf</code> and NaN propagates; no exception is raised for those. Because the result is approximate, callers should compare with a tolerance, not for exact equality \u2014 the floating-point analogue of CKKS's approximate arithmetic (see :func:<code>addlib.testing.approx_equal</code>).</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float</code> <p>First addend (int or float).</p> required <code>b</code> <code>float</code> <p>Second addend (int or float).</p> required <p>Returns:</p> Type Description <code>float</code> <p>The IEEE-754 double-precision sum.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If an argument is not a real number.</p> Example <p>from addlib import add_floats add_floats(0.1, 0.2) 0.30000000000000004</p> Source code in <code>python/src/addlib/core.py</code> <pre><code>def add_floats(a: float, b: float) -&gt; float:\n    \"\"\"Return ``a + b`` for two real numbers, computed in C as IEEE-754 doubles.\n\n    Follows IEEE-754 semantics: ``add_floats(float(\"inf\"), 1.0)`` is ``inf`` and\n    NaN propagates; no exception is raised for those. Because the result is\n    approximate, callers should compare with a tolerance, not for exact\n    equality \u2014 the floating-point analogue of CKKS's approximate arithmetic\n    (see :func:`addlib.testing.approx_equal`).\n\n    Args:\n        a: First addend (int or float).\n        b: Second addend (int or float).\n\n    Returns:\n        The IEEE-754 double-precision sum.\n\n    Raises:\n        TypeError: If an argument is not a real number.\n\n    Example:\n        &gt;&gt;&gt; from addlib import add_floats\n        &gt;&gt;&gt; add_floats(0.1, 0.2)\n        0.30000000000000004\n    \"\"\"\n    af = _as_float(\"a\", a)\n    bf = _as_float(\"b\", b)\n    return float(lib.add_f64(af, bf))\n</code></pre>"},{"location":"reference/python-api/#addlib.c_version","title":"addlib.c_version","text":"<pre><code>c_version()\n</code></pre> <p>Return the version string reported by the underlying C core.</p> <p>Useful for asserting that the loaded native extension matches the Python package version.</p> Source code in <code>python/src/addlib/core.py</code> <pre><code>def c_version() -&gt; str:\n    \"\"\"Return the version string reported by the underlying C core.\n\n    Useful for asserting that the loaded native extension matches the Python\n    package version.\n    \"\"\"\n    raw: bytes = bytes(ffi.string(lib.add_version()))\n    return raw.decode(\"ascii\")\n</code></pre>"},{"location":"reference/python-api/#exceptions","title":"Exceptions","text":""},{"location":"reference/python-api/#addlib.core.AdditionOverflowError","title":"addlib.core.AdditionOverflowError","text":"<p>               Bases: <code>OverflowError</code></p> <p>Raised when an integer addition cannot be represented in signed 64 bits.</p> <p>Subclasses the built-in :class:<code>OverflowError</code>, so existing <code>except OverflowError</code> handlers keep working.</p> Source code in <code>python/src/addlib/core.py</code> <pre><code>class AdditionOverflowError(OverflowError):\n    \"\"\"Raised when an integer addition cannot be represented in signed 64 bits.\n\n    Subclasses the built-in :class:`OverflowError`, so existing\n    ``except OverflowError`` handlers keep working.\n    \"\"\"\n</code></pre>"},{"location":"reference/python-api/#test-helpers","title":"Test helpers","text":""},{"location":"reference/python-api/#addlib.testing.approx_equal","title":"addlib.testing.approx_equal","text":"<pre><code>approx_equal(a, b, *, rel_tol=DEFAULT_REL_TOL, abs_tol=DEFAULT_ABS_TOL)\n</code></pre> <p>Return True if <code>a</code> and <code>b</code> are equal within tolerance.</p> <p>NaN is never equal to anything (including itself). Infinities are equal only to the same infinity. Otherwise this is :func:<code>math.isclose</code> with the project's default tolerances.</p> Source code in <code>python/src/addlib/testing.py</code> <pre><code>def approx_equal(\n    a: float,\n    b: float,\n    *,\n    rel_tol: float = DEFAULT_REL_TOL,\n    abs_tol: float = DEFAULT_ABS_TOL,\n) -&gt; bool:\n    \"\"\"Return True if ``a`` and ``b`` are equal within tolerance.\n\n    NaN is never equal to anything (including itself). Infinities are equal only\n    to the same infinity. Otherwise this is :func:`math.isclose` with the\n    project's default tolerances.\n    \"\"\"\n    if math.isnan(a) or math.isnan(b):\n        return False\n    if math.isinf(a) or math.isinf(b):\n        return a == b\n    return math.isclose(a, b, rel_tol=rel_tol, abs_tol=abs_tol)\n</code></pre>"},{"location":"rfcs/","title":"RFCs (Requests for Comments)","text":"<p>A lightweight design-proposal process for protocol-level, public-API, or breaking changes (roadmap Phase 7.3). Small fixes and additions don't need an RFC \u2014 just open a PR. RFCs exist so big decisions are discussed in the open and leave a durable rationale trail (which doubles as bus-factor insurance).</p>"},{"location":"rfcs/#when-you-need-one","title":"When you need one","text":"<ul> <li>A breaking change to the Python API or the C ABI.</li> <li>A new published artifact, or a change to the artifact/versioning strategy.</li> <li>Anything you'd want a second maintainer to sign off on the design before   code is written.</li> </ul>"},{"location":"rfcs/#process","title":"Process","text":"<ol> <li>Copy <code>0000-template.md</code> to    <code>NNNN-short-title.md</code> (use the next free number).</li> <li>Open a PR adding it. Discussion happens on the PR.</li> <li>A maintainer other than the author must approve. Contested RFCs are decided    per GOVERNANCE.md    (lead maintainer breaks ties).</li> <li>On acceptance, set <code>Status: Accepted</code> and merge. Significant accepted RFCs may    also be distilled into an ADR.</li> </ol>"},{"location":"rfcs/#index","title":"Index","text":"RFC Title Status \u2014 none yet \u2014"},{"location":"rfcs/0000-template/","title":"RFC-NNNN: &lt;a class=\"headerlink\" href=\"#rfc-nnnn\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h1&gt; &lt;ul&gt; &lt;li&gt;Status: Draft  &lt;!-- Draft | Accepted | Rejected | Superseded by RFC-XXXX --&gt;&lt;/li&gt; &lt;li&gt;Author(s): &lt;name / @handle&gt;&lt;/li&gt; &lt;li&gt;Created: YYYY-MM-DD&lt;/li&gt; &lt;li&gt;Tracking issue / PR: #NNN&lt;/li&gt; &lt;/ul&gt; &lt;h2 id=\"summary\"&gt;Summary&lt;a class=\"headerlink\" href=\"#summary\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;One paragraph: what is being proposed?&lt;/p&gt; &lt;h2 id=\"motivation\"&gt;Motivation&lt;a class=\"headerlink\" href=\"#motivation\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;What problem does this solve? Who is affected? Why now? What is the cost of &lt;em&gt;not&lt;/em&gt; doing it?&lt;/p&gt; &lt;h2 id=\"design\"&gt;Design&lt;a class=\"headerlink\" href=\"#design\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;The concrete proposal. Include the API/ABI surface, examples, and how it interacts with the &lt;a href=\"../../explanation/design/#5-stability-tiering\"&gt;stability tiering&lt;/a&gt;. If it touches the C ABI, state the &lt;code&gt;SOVERSION&lt;/code&gt; / compatibility impact.&lt;/p&gt; &lt;h2 id=\"backward-compatibility\"&gt;Backward compatibility&lt;a class=\"headerlink\" href=\"#backward-compatibility\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;Is this breaking? Migration path? Deprecation plan (see &lt;a href=\"../../explanation/versioning/\"&gt;versioning&lt;/a&gt;)?&lt;/p&gt; &lt;h2 id=\"alternatives-considered\"&gt;Alternatives considered&lt;a class=\"headerlink\" href=\"#alternatives-considered\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;What else could solve this, and why is the proposed design better?&lt;/p&gt; &lt;h2 id=\"drawbacks-risks\"&gt;Drawbacks &amp; risks&lt;a class=\"headerlink\" href=\"#drawbacks-risks\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;Honest downsides, including maintenance burden.&lt;/p&gt; &lt;h2 id=\"unresolved-questions\"&gt;Unresolved questions&lt;a class=\"headerlink\" href=\"#unresolved-questions\" title=\"Permanent link\"&gt;\u00b6&lt;/a&gt;&lt;/h2&gt; &lt;p&gt;What is still open and should be settled during review?&lt;/p&gt;","text":""},{"location":"tutorials/getting-started/","title":"Getting started","text":"<p>Your first addition in five minutes. This is a learning-oriented tutorial \u2014 follow it top to bottom.</p>"},{"location":"tutorials/getting-started/#1-install","title":"1. Install","text":"<pre><code>pip install addlib\n</code></pre> <p>No C compiler is needed: the wheel already contains the compiled <code>libadd</code> core.</p>"},{"location":"tutorials/getting-started/#2-add-two-integers","title":"2. Add two integers","text":"<pre><code>from addlib import add\n\nprint(add(2, 3))      # 5\nprint(add(-10, 4))    # -6\n</code></pre> <p><code>add</code> works on signed 64-bit integers and is computed in C. It is checked: if the result would not fit in 64 bits, it raises rather than wrapping.</p> <pre><code>from addlib import add, AdditionOverflowError\n\ntry:\n    add(2**63 - 1, 1)\nexcept AdditionOverflowError as exc:\n    print(\"too big:\", exc)\n</code></pre>"},{"location":"tutorials/getting-started/#3-add-two-floats","title":"3. Add two floats","text":"<pre><code>from addlib import add_floats\n\nprint(add_floats(0.5, 0.25))   # 0.75\nprint(add_floats(0.1, 0.2))    # 0.30000000000000004  (IEEE-754 rounding)\n</code></pre> <p>Float results are approximate. Compare them with a tolerance, never with <code>==</code>:</p> <pre><code>from addlib.testing import approx_equal\nassert approx_equal(add_floats(0.1, 0.2), 0.3)\n</code></pre>"},{"location":"tutorials/getting-started/#4-check-the-versions-line-up","title":"4. Check the versions line up","text":"<pre><code>import addlib\nprint(addlib.__version__, addlib.c_version())   # 0.1.0 0.1.0\n</code></pre> <p>The Python package and the native core report the same version \u2014 a test enforces this so they can never drift.</p>"},{"location":"tutorials/getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Handle overflow safely</li> <li>Use <code>libadd</code> directly from C</li> <li>Why the API is shaped this way</li> </ul>"}]}